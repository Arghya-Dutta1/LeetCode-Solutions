class Solution {
public:
    void reverseString(vector<char>& s) {
        ranges::reverse(s);
    }
};

static const int __ = [](){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    return 0;
}();